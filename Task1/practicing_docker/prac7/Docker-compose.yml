version: '3'

services:
  phpapp:
    build:
      context: ./
      dockerfile: Dockerfile
    image: php:7.2-apache
    ports:
    - "9040:80"
    volumes:
    - "./:/var/www/html"
    container_name: trying
